version: "2"

linters:
  exclusions:
    rules:
      # The usage strings end in "..." because that is the argument syntax,
      # not a sentence ending in punctuation.
      - linters: [staticcheck]
        text: "ST1005"
        path: internal/cli/
